<!DOCTYPE html>
<html>
    <head>
        <meta charset='utf-8'>
        <meta name='viewport' content='width=device-width, initial-scale=1.0, maximum-scale=1.0'>
        <title>Me - Login</title>
        <!-- TODO: Local installation of Fomantic and jQuery -->
        <link rel='stylesheet' type='text/css' href='https://cdn.jsdelivr.net/npm/fomantic-ui@2.8.4/dist/semantic.min.css'>
        <link rel='stylesheet' href='/{base_url}/css/me-app-login.css' />
        <!-- Fonts -->
        <link href='https://fonts.googleapis.com/css2?family=Nunito+Sans&display=swap' rel='stylesheet'>
        <link href='https://fonts.googleapis.com/css2?family=Roboto&display=swap' rel='stylesheet'>
        <script src='https://cdnjs.cloudflare.com/ajax/libs/jquery/3.4.1/jquery.js'></script>
        <script src='https://cdn.jsdelivr.net/npm/fomantic-ui@2.8.4/dist/semantic.min.js'></script>
        <script src='https://cdn.jsdelivr.net/npm/vue/dist/vue.js'></script>
    </head>

    <body>
        <div id='me-loginform'>
            <div class='ui middle aligned center aligned grid'>
                <div class='column'>

                    <div class='ui raised card' id='login'>
                        <h3 class='ui inverted header'>{{ texts.header }}</h3>

                        <form class='ui form' v-on:submit.prevent='login'>
                            <div class='field' v-bind:class='{ disabled: status_waiting }'>
                                <label>{{ texts.field_username }}</label>
                                <div class='ui left icon input'>
                                    <input type='text' ref='username' id='username' v-model='fields.username' v-bind:placeholder='texts.field_username_placeholder' v-on:keypress.enter.prevent='focus("password")'>
                                    <i class='user icon'></i>
                                </div>
                            </div>
                            <div class='field' v-bind:class='{ disabled: status_waiting }'>
                                <label>{{ texts.field_password }}</label>
                                <div class='ui left icon input'>
                                    <input type='password' ref='password' id='password' v-model='fields.password' v-bind:placeholder='texts.field_password_placeholder'>
                                    <i class='lock icon'></i>
                                </div>
                            </div>
                            <button class='fluid ui primary button' v-bind:class='{ loading: status_waiting }'>{{ texts.button_login }}</button>
                        </form>

                    </div>

                </div>
            </div>
        </div>
        <script src='/{base_url}/js/me-app-login.js'></script>
    </body>
</html>
